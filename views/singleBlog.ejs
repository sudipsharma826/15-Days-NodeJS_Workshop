<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= blog ? blog.title : 'No Blog Found' %></title>
    <link rel="stylesheet" href="/singleBlog.css">
</head>
<body>
    <% if (blog) { %>
    <div class="blog-card">
        <div class="blog-image">
            <img src="http://localhost:3000/<%= blog.image %>" alt="Blog Image">
        </div>
        <div class="blog-content">
            <h1><%= blog.title %></h1>
            <h2><%= blog.subtitle %></h2>
            <p><%= blog.description %></p>
        </div>
        <a href="/delete/<%= blog.id %>" class="delete-button ">Delete</a>
    </div>
    <% } else { %>
    <p>No blog data found.</p>
    <% } %>
</body>
</html>
